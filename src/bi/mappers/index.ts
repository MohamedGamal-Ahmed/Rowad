/**
 * BI Layer — Mappers (RESERVED)
 *
 * Reserved for future phases (translating ExecutivePortfolioRow / future
 * datasets into consumer-specific shapes — Power BI schema, Excel sheets,
 * REST DTOs, etc.). Intentionally empty per Sprint 5.0 Implementation Order —
 * only Phases 1-5 (folder structure, DTO, calculators, builder, service
 * contracts) are in scope. Do not add logic here without CTO approval.
 */
export {};
